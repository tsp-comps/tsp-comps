import tsplib95
import networkx as nx

def process_tsp95(file_path):
    tsp_set = tsplib95.load(file_path)
    g_tsp = tsp_set.get_graph()
    return g_tsp

def main():

    number = input("Your sentence: ") # user input
    if len(errorful_sentence) == 0: # if tsp95, then go there
        

    return

if __name__=="__main__":
    main()